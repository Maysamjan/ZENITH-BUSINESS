"""Core foundation: identity, configuration, paths, logging, errors, i18n.

This package holds cross-cutting infrastructure that every other layer depends
on. It must never import UI, database business tables, or business services.
"""
