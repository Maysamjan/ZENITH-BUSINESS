"""Test-only tooling. NOTHING in this package is part of the application.

It lives under ``tests/`` deliberately: the PyInstaller spec collects
``zenith_business`` only, so nothing here can reach a production build.
"""
